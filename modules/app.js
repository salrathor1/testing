let coder = true;

const add = (num1, num2) => {
    return num1 + num2;
}

const subtract = (num1, num2) => {
    return num1 - num2;
}

let someNames = ["Ann", "Ben", "Charlie"];

module.exports = {
    add, 
    subtract,
    someNames,
    coder
}